server:
  port: 9093

spring:
  application:
    name: RATING-SERCIVE
  datasource:
      url: jdbc:mysql://localhost:3306/microservice2
      username: root
      password: sumit
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
      hibernate:
        ddl-auto: update   
  show-sql: true       
  properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        new_generator_mappings: false
  config:
     import:
     - optional:configserver:http://localhost:9095

okta:
  oauth2:
    issuer: https://dev-77584981.okta.com/oauth2/default
    audience: api://default

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#
